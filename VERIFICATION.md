# 本地验收记录

日期：2026-09-20。已接入手绘庭院画风。保留十关、中点触发炸弹与独占格子规则；没有根号门，保留根式敌人与平方门。

## 自动验证

- TypeScript 严格类型检查通过。
- 85 项自动测试通过。
- 交付前修复新增 10 项回归测试：离线重复挑战保留最高星与最快时间、跨关卡顺序补传、旧单条缓存兼容、不同关卡响应两种乱序、部分失败保留余项、中途切换账号、过滤旧存档版本及损坏条目；固定图片缓存策略与替换后读取新内容；全部运行图片和 CSS 图片使用新地址避开旧强缓存。
- 实际前端保存与补传函数通过真实 HTTP 接口写入独立内存 SQLite：多次离线成绩恢复后，第 1 关保留 3 星 / 70 秒、第 2 关保留 2 星 / 110 秒，待同步缓存清空。正式构建中的 26 张图片均返回 200 与 `no-cache`，带内容哈希的 JS/CSS 保留长期缓存。临时服务已关闭，未操作正式服务或数据库。
- 新增 8 项音效回归测试：14 种波形无非有限数值、非静音、首尾淡入淡出、峰值留有余量、可复现；交互后解锁、缓存与混音压缩；首次发射与重复事件限流；并发上限和节点释放；关闭时停止正在播放的声音、偏好保存与重新开启；已保存的静音状态；音频不可用或自动播放受限时不阻断游戏；全部战斗事件的播放适配。
- 新增 5 项美术测试：19 张角色图片齐全且为 256×256 RGBA、样式引用资源存在、图片加载失败后重试与缓存、七种正确函数标牌与卡图缓存、敌人图片不写死公式、素材失败页的手册入口安全禁用（部分断言合并在同一测试中）。
- 移除根号门后已重新通过全部自动测试与正式构建；现有十关解法不依赖根号门。
- 九类函数的 720 组随机模板验证通过；另按十关各自的函数池和幂次范围验证每种模板的 80 个随机种子，标准解法所需卡牌均已在对应关卡解锁。
- 十关各 8 个随机种子，共 80 场完整战斗模拟，使用正常资源、冷却、攻击和逐步增建的防线，全部三星通关；通关路线无需使用炸弹。
- 数学测试覆盖定义域、主值区间、链式法则、恒等式与非法运算。
- 战斗测试覆盖基础两格射程、多塔连续继承、跨行与间隔限制、回收和摧毁后的射程回落。
- 补种测试覆盖右半格、恰好中点、已过中点、下一名敌人、一门一次、门先于子弹、补塔不推回敌人。
- 炸弹测试覆盖三种范围、不伤己方、费用与冷却、解锁、暂停种植、禁止回收和终局禁用。
- 三档炸弹均验证空地等待、仅有敌人在爆炸范围内不触发、经过所在格中点才引爆、已过中点不补触发、等待下一名敌人，以及与塔、门、其他炸弹互斥占格。放置失败不扣费、不启动冷却。
- 爆炸不重复奖励，被炸死的敌人不会在同一次更新中继续啃塔或触发其他炸弹，未触发的炸弹保留。
- 二十次幂敌人对照测试：单塔被毁并漏怪，三座联动塔可保住前塔并消灭敌人。
- HTTP 测试覆盖注册、重复用户名、错误密码、会话退出、1–10 关逐关提交、越级提交、第 11 关拒绝、跨来源请求、账号数据隔离和公开目录隔离。
- SQLite 旧三关表结构升级测试保留账号密码哈希、有效会话、星数和最快用时，可保存新关卡；关闭后重开记录保持一致。
- 正式构建成功，前端资源已压缩；游戏不依赖第三方字体或在线素材。
- 本机用户管理覆盖搜索、改名、重置密码、精确覆盖成绩、退出登录、清空进度、删除账号及关联记录清理，操作不影响其他用户。
- 管理接口拒绝缺少令牌、跨来源、伪造主机、转发请求、无效输入和错误的用户名确认；监听公共地址时关闭管理页面与接口。已开始但未提交完的成绩请求在管理重置后也不能恢复旧进度。
- 删除最高编号账号、清空所有账号并重开数据库后，新账号都不复用已删除编号；旧管理页面针对已删除账号的操作返回 404。
- 数据库版本 0、1、2 升级后保留用户编号、用户名、密码哈希、盐值、注册日期、有效会话和成绩；存档版本持久化，删除时仍能正确清理关联记录。
- 管理员修改或清空成绩后，旧版本及不带版本的补传返回 409；重新登录后产生的新版本成绩正常保存，其他账号的成绩不能借用当前登录提交。
- 直接运行前端实际保存与补传函数，以受控网络响应和页面替身验证：切换账号、同账号重新登录、两场相同成绩按不同顺序返回、旧请求报错，均不污染新会话、误删新缓存或改动新对局按钮；断网缓存可正常补传，过期或损坏缓存会清除。
- 数学显示验证系数乘倒数、多个分母、负号、括号及多因子乘积；例如 `ln(2x + 1)` 的导数显示为 `2/(2x + 1)`，常用 `3x` 写法保留。

## 浏览器操作验证

### 本次原创音效

- 使用不连接游戏数据库的独立临时页面，直接加载实际音效代码。浏览器交互后 AudioContext 进入 running，14 种音效逐个播放完成，实时分析器测到非零波形，播放结束后活动声音节点归零。
- 播放过程中关闭音效，活动节点归零，后续事件不再发声；重新开启正常。浏览器没有记录 JavaScript 错误。测试页和临时服务已关闭，未操作正式服务或存档。
- 15 秒试听导出到 `output/audio/garden-sounds-preview.wav`，按种植、回收、发射、命中、变换、无效、消灭、爆炸、啃咬、摧毁、漏怪、波次、胜利、失败排列。试听为独立声音串联；游戏内叠加播放另有动态压缩。自动验证和波形测量不能替代用户对音色的主观试听。

### 本次手绘画风

- 使用独立内存数据库与临时测试账号验收，不改正式数据库。
- 桌面 1440×900：登录页、大厅、十关卡片、运算与敌人手册显示正常；新图片、木纸面板和动态函数标牌已接入。
- 第 10 关显示 10 张卡牌。准备阶段在 A/C/E 三行种下共 9 座求导塔与 2 道对数门，种植位置与棋盘一致；开战后敌人移动、求导弹和动态公式正常显示。
- 暂停战斗后，范围炸弹正常落在 B 行第 5 列，能量扣除 350，冷却停在 45 秒；炸弹留在格中，没有因为周围已有敌人而立刻爆炸。
- 窄窗口 390×844：登录/注册、大厅、战场和离开确认框正常排版；修正顶部按钮挤压，卡牌栏可以横向滚动到三档炸弹及回收工具。大厅页面没有整体横向溢出。
- 本轮浏览器未记录 JavaScript 错误。结算弹窗沿用统一纸张样式，本轮未手动逐关通关或单独触发全部结算状态。

### 此前功能验收

此前四项缺陷修复使用自动回归测试验证；以下保留历史功能验收记录。

- 使用独立内存数据库和预置测试账号登录，大厅显示 01–10 关、30 星总量和第 10 关的正确标题。
- 移除根号门前，第 10 关包含 11 张卡牌，准备阶段三档炸弹禁用；窄窗口中卡牌栏横向滚动后可操作全部卡牌。移除后为 10 张卡牌，本次未重复浏览器操作验证。
- 在同一行第 4、2、1 列放三座求导塔，扣除 300 能量，后排联动射程高亮延伸到第 6 列。
- 暂停后在正弦敌人占据格子的右半边补反正弦门，恢复后门被消耗，变换后的敌人被炮台消灭。
- 本次炸弹修订使用独立内存数据库和测试账号进入第 2 关：在 A 行第 1 列放塔后，尝试重叠放炸弹显示“这个格子已经有单位”，炸弹不进入冷却。
- 暂停时在 A 行第 6 列种下单格炸弹，能量从 606 减到 406，冷却为 30 秒。恢复战斗后炸弹仍在格子中，远处敌人继续移动；敌人经过后炸弹被消耗，本波剩余数量从 5 减到 4，己方炮台保留。
- 检查过程中浏览器未记录 JavaScript 错误。三档范围及中点边界的完整组合由自动测试覆盖。
- 用户管理使用独立内存数据库验证：搜索定位玩家、改名后更新列表、将成绩从 3 星 / 62 秒改为 1 星 / 123.5 秒，以及清空和删除的用户名确认弹窗、取消保留数据。页面布局正常，无浏览器 JavaScript 错误。密码重置和实际删除由独立数据库接口测试验证，未操作正式账号。

## 交付边界

十关完整通关使用纯战斗模拟验证；本次浏览器验收覆盖新增功能，未逐关完成人工鼠标通关。主要测试目标是电脑浏览器，窄窗口验证了卡牌滚动。当前验证说明存在合法通关路线，不代表难度已完成人工体验调优；新增函数或修改平衡时应重新运行相应测试。

运行时 `data` 目录中的账号与进度是本地数据。自动测试使用独立的临时数据库，本次界面验收使用独立的内存数据库，未改动正式存档。正式服务重启后会自动升级存档表结构。
